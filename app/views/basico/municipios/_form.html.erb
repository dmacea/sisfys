<%= form_for(@basico_municipio) do |f| %>
  <% if @basico_municipio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@basico_municipio.errors.count, "error") %> prohibited this basico_municipio from being saved:</h2>

      <ul>
      <% @basico_municipio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :estado_id %><br>
    <%= f.text_field :estado_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
